on: [issues]

jobs:
  questions:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Check out repository
      uses: actions/checkout@v2
    - name: Hello world action step
      uses: ./.github/actions/questions
      with:
        num-questions: '12'
